<?php

declare(strict_types=1);

/*
 * API routes untuk modul AccessControl (User + Role + Module).
 * Auto-prefixed dengan /api oleh bootstrap (lihat routes/api.php).
 *
 * Belum ada endpoint API — semua interaksi lewat web (Inertia).
 * Tambahkan route di sini (mis. auth:sanctum) saat butuh akses via token.
 */
